# ISSUE-002: 加密房间 TTL 过期销毁后客户端永久卡死

- **状态**: done
- **优先级**: P0
- **区域**: public/js/app.js

## 问题

服务端删密码后 close(4000)，客户端重连后 isSecureRoom 仍 true，永远等 challenge，不发 join，显示已连接但看不到任何设备。

## 修复方案

处理 4000 关闭码：清密码状态+提示+直接重连；"等待 challenge"加超时兜底(8s 未 joined 则重置重连)。
